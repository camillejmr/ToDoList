{% block extrastyles %}
    <link rel="stylesheet" href="{{ asset('css/inc/nav.css') }}">{% endblock %}
<div class="nav">
    <input type="checkbox" id="nav-check">
    <div class="nav-header">
        <div class="nav-title">
            <a href="{{ path('main_home') }}">
                My Todo App</a>
        </div>
    </div>
    <div class="nav-btn">
        <label for="nav-check">
            <span></span>
            <span></span>
            <span></span>
        </label>
    </div>

    <div class="nav-links">{% if app.user %}
            Bienvenue {{ app.user.prenom }}
        <a href="{{ path('app_logout') }}" title="Logout">Se déconnecter</a>{% endif %}
        <a href="{{ path('main_home') }}" title="Home">Accueil</a>
        <a href="{{ path('listes_listes') }}" title="Lists">Toutes les listes</a>
        <a href="{{ path('listes_create') }}" title="Create">Créer une liste</a>
        {% if not app.user %} <a href="{{ path('app_register') }}" title="Create account">Créer un compte</a>
        <a href="{{ path('login') }}" title="Create account">Se connecter</a>{% endif %}
    </div>
</div>

